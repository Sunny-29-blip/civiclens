{
  "name": "civiclens",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --no-audit --no-fund && npm run build && rm -rf ../dist && cp -r dist ../dist"
  }
}
